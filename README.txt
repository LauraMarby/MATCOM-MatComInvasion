>poner en la terminal 'bash Makefile' para generar el archivo del juego
>poner en la terminal 'bash Install' para llevar el archivo del juego a la carpeta bin 
del sistema(así se puede abrir desde cualquier dirección en la terminal)
>poner en la terminal 'MatComInvasion' para ejecutar el juego